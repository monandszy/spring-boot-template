services:
  rabbitmq:
    image: rabbitmq:${RABBITMQ_VERSION}
    container_name: data-rabbitmq
    hostname: rabbitmq
    restart: unless-stopped
    env_file: .env
    ports:
      - "5672:5672"
      - "15672:15672"
